---
import '../style/global.css';
import Cursor from '../components/Cursor.astro';
import Layout from './Layout.astro';
import type { string } from 'astro:schema';

export interface Props {
  name: string;
  subsection?: boolean;
  active?: string;
  subsectionActive?: number;
  darkbg?: string
}
const { name, active, subsection = false, subsectionActive = 0, darkbg="none" } = Astro.props as Props;

const title = name + " – interdependence"; 

---

<Layout subsection={subsection} active={active} subsectionActive={subsectionActive} title = {title} hideHeaderWhileScrollingMobile={true}>
    <div class="main-container darkbg-high ">
        <slot/>
    </div>

</Layout>

<style>
    .main-container {
        position: relative;
        width: 80%;
        margin: auto;
        max-width: 1200px;
        min-height: 100vh;
        padding-bottom: min(15rem, 18%);
    }

    /* .main-container.darkbg-high {
        transition: all 1s ease;
        background-position: 0% 0%;

        /* background-color: aqua; */
     

    @media (max-width: 768px) {
    .main-container {
        width: 100%;
        padding: min(5rem, 6%) 15px;
    }
}


</style>